# Condition 2: domain + real general — replay mitigation with real data.
name: domain_general
total_samples: 10000
max_len: 1024
corpora:
  - {role: domain,  spec: "hf:MedRAG/textbooks:train:content", weight: 0.5}
  - {role: general, spec: "hf:Salesforce/wikitext:wikitext-103-raw-v1:train:text", weight: 0.5}
