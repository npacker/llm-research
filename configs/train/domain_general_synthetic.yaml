# Condition 4: domain + real general + synthetic (augmentation).
name: domain_general_synthetic
total_samples: 10000
max_len: 1024
corpora:
  - {role: domain,    spec: "hf:MedRAG/textbooks:train:content", weight: 0.5}
  - {role: general,   spec: "hf:NeelNanda/pile-10k:train:text", weight: 0.25}
  - {role: synthetic, spec: null, weight: 0.25}
